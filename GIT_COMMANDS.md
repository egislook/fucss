```javascript
//if someone released or pushed other version
$ git pull origin master
$ git pull origin --tags

// commiting
$ git status
$ git add .
$ git commit -m “commit message”
$ git push origin master

// releasing
$ git tag 0.5.3
$ git push origin --tags

$uglifyjs fucss.js > fucss.min.js
```